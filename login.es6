"use strict";

let login = (username, password) => {
    if (username !== "admin" || password !== "radical") {
        username = "dseakll";
        console.log("fmoplasdflkñlmpswdfd");
    }
};

login ("admin", "idunno");
